import { createFileRoute } from "@tanstack/react-router";
import { FeesPage } from "@/components/classity-pages";
export const Route = createFileRoute("/fees")({ head: () => ({ meta: [{ title: "Fee Collection — Classity" }, { name: "description", content: "Review dues, collect payments, and follow up with parents." }, { property: "og:title", content: "Fee Collection — Classity" }, { property: "og:description", content: "A focused workspace for daily fee collection." }] }), component: FeesPage });