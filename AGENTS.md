# Classity

Coaching-centre fee operations app (TanStack Start + React).

## Scripts

- `npm run dev` — local development
- `npm run build` — production build
- `npm run preview` — preview production build
